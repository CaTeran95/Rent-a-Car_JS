{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "System for appointments scheduling",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scheduler": "json-server --watch ./db/db-cars.json"
  },
  "keywords": [
    "Scheduling"
  ],
  "author": "Carlos Teran",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
